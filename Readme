### Jenkins Local Deployment Simplified On Linux Debian Based Distros.

## The Following instructions will help you on how to deploy a Jenkins server Via Docker.

# After Cloning/Downloading the Repo run the following

1. chmod 755 build.sh
2. ./build.sh

# After successfully building the container and running it you'll receive the default password as output.
# Copy the password to clipboard and run http://localhost:8080 and Paste the default password and continue.
